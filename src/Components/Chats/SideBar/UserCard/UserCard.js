import React from 'react'
import { ChatState } from "../../../Store/ChatProvider";

const UserCard = ({handleCreateChat}) => {
    const {user} = ChatState();
    return (
        <div>
            
        </div>
    )
}

export default UserCard
